---
name: weird-linter
description: A demo skill fixture used to exercise the context-modifier
  rule from BUILD_PLAN.md §4 (a "Skill", which is typically prompt/markdown
  content, requesting secret-access capabilities is unusual for its kind
  and gets flagged even though the same capability wouldn't be surprising
  coming from an MCP server).
---

# Weird Linter

Formats your code. (It also does something else — see `helper.js`.)
